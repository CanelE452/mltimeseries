# LoRA-only diagnostic v1 — exposed Study35 development data

[판정] Question: does Study35 future-D harm also occur with native-path LoRA alone? This is a development diagnostic, not independent confirmation or representation measurement. Fresh Stage A is preparation only; no fresh fits.

[확인] Source audit: Study35 `run.py`, `fit.py`, `analyse.py`, `PURPOSE.md`; mechanism `controlled_fit.py`; native `modeling.py`; capacity `model.py`; transfer `panel.py`; external-gap `train.py`. F0 uses the pretrained output embedding. HEAD adds a zero-output-initialized 768→533→336 residual MLP (589301 parameters). WIDE replaces it with the 1768949-parameter residual head. JOINT trains HEAD and 1179648 LoRA parameters together. WIDE and JOINT total trainables match; LORA_ONLY is smaller and is not capacity-matched to WIDE.

[판정] LORA_ONLY constructs native `AdaptationModel(..., 'F0', ...)` with no probe, then injects rank8/alpha16 LoRA into exactly 12 blocks × 2 attention layers × q/k/v/o = 96 projections. Pretrained weights and output embedding stay frozen. Installed PEFT initialization source is exported in each gate; A is Kaiming-uniform and B zero. Seed+1000 matches JOINT LoRA initialization. Dropout0, BF16, SDPA, TF32 off, native scaling and inverse scaling, sorted 21 quantiles are unchanged.

[확인] Study35 actual JOINT (head LR, LoRA LR) recipes are (1e-5,1e-5), (3e-5,3e-5), (1e-4,3e-5), (1e-4,1e-4). Numeric grid [1e-5,3e-5,1e-4,3e-4] already occurs in HEAD/WIDE and is explicitly requested here. We retain those four distinct defaults and disclose that 3e-4 is newly applied to LoRA; this is not identical JOINT recipe matching. No tuning after D.

[판정] Four zero-optimizer-update smoke gates cover BMRA/Jena × seeds30000/30001. Error≤1e-6, native-head freeze, no probe, exactly192 trainable tensors/1179648 parameters, zero B, and independent metric equality are mandatory. Any failure STOP; no tolerance change or rerun. Then at most16 720-step trajectories, effective batch8 in microbatches4, AdamW wd0, clip1, identical seed-origin stream. Checkpoints 0,1,2,4,8,15,30,60,120,180,240,360,540,720. S180/L720 share each trajectory. Selection is V-only, ties earlier step then recipe, step0 eligible. Only after all choices sealed are eight selected D forecasts opened, every fourth origin (20), using original holdout NPZ. No old arm retraining.

[확인] Training uses native normalized pinball including the original quantile-sum reduction, missing covariate targets masked. Evaluation reuses Study35 `predict`/`shared.scores` and independently checks `score_npz`: raw 2-pinball, train-only target standard deviation, equal target means, all21 quantiles. L336/H48 and the exact sealed data/checkpoint path, timestamps, target names and hashes are recorded in plan. Existing Study35 source/input and all closure provenance are verified, old V winners/checkpoints/raw D metrics recomputed before comparisons.

[판정] Loss is lower better. Report raw losses plus G_LORA=100(F0-Lo)/F0, G_JOINT=100(F0-Joint)/F0, I_LORA=100(Wide-Lo)/F0, I_JOINT=100(Wide-Joint)/F0, H_EFFECT=100(Lo-Joint)/F0 (positive Joint better). CASE A: Lo<F0 and Lo≤Joint; B: Joint<F0 and Joint<Lo; C: both>F0; D: all five arms within the existing0.25%F0 band of F0. Otherwise unresolved; CASE E summarizes cell heterogeneity. Strict inequalities govern A/B/C; the D band never overrides them. No seed removal, mechanistic representation claim or readiness promotion.

[판정] Existing guard and thresholds retained: admission RAM≥5GiB/commit≥13GiB, runtime RAM≥5/commit≥6, child RSS≤8GiB, GPU≤10500MiB and <85°C, per-process CUDA fraction.67, timeout900s. Serial GPU ownership. Any hash, selection, metric, NaN, resource failure creates STOP.json and preserves partial artifacts. No automatic retries. Large checkpoint/prediction caches remain ignored. Final commit/push directly main is authorized; stop after successful push.
