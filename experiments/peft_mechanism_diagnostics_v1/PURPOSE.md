# Study25: mechanism diagnostics

Authorized by user on 2026-09-10: execute roadmap R1, R2, R3 sequentially, with the stated evidence gates. Root owns reference diagnostic, controlled_fit, run_controlled and integrated reporting. Data-engineer owns hospital_diagnostic.py and results/hospital. Existing studies and safety guard remain unchanged.

R1: Study20 exposed-E descriptive decomposition, then matched native-frozen-head + zero-initialized residual Linear/MLP. Fit arms LINEAR, MLP, ALL(attention-only rank8 + same MLP), 2 datasets, 2 seeds 25000/25001, 2 optimizer recipes, 200 updates, validation every40, micro4/effective8. Recipes (head LR, LoRA LR)=(1e-4,1e-4),(3e-4,3e-5). Same two head learning rates in frozen and adapted arms; two paired optimization recipes, not exhaustive independent two-dimensional HPO. Select mean-V recipe across seeds and per-fit best checkpoint before E. LR optimality is not guaranteed. Smoke3 updates for three arm types uses train-only pseudo-validation. Native Study20 F0/HEAD_ONLY/LORA remain historical reference; matched controls are new interventions. No new dataset or independent test is claimed.

R2 entry fixed before new results: Bike matched MLP minus ALL gain positive in both seeds and mean>1% of historical F0 loss. This is an operational pilot gate, not a significance test. Conditional 12 fits: early/middle/late four-block groups and 2 random four-block groups at rank6; all12 rank2 has equal adapter parameter budget. Random groups generated by stdlib Random(25099)/(25100) before fitting. Fixed recipe from R1 ALL's mean-V choice; same head, seeds and steps. Location chosen among early/middle/late using mean V before any placement E. All E figures descriptive, previously exposed. Rank8 all-layer arm is a larger-budget reference. No claim that loss-only location scanning is a validated probe-based selector.

R3: saved Hospital alpha risk curves and stability audit only. No new alpha tuning or GPU. E-based oracle diagnostic only. Longitudinal noise/shift identification may be limited by one V2 year and two E years.

Safety: GPU serial, admission13GiB free commit /5GiB RAM twice5s apart; unchanged runtime guard6GiB commit/5GiB RAM/85C and per-backward commit check. Per-child900s timeout. No automatic retry of failed output directories. Protected input hash, initial F0 equality, frozen parameter hash, nonzero gradient, and exact checkpoint replay checked. No automatic method expansion beyond this protocol.

Scope: mechanism hypotheses require stronger independent data and targeted interventions after these diagnostics. Raw entropy association does not establish a causal spectral bottleneck. Structure-manipulation R1-2 is not triggered without a supporting diagnostic signature. No commit/push requested.
