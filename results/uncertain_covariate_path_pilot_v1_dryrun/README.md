# UCP-PATH-PILOT-v1 dry run

Plumbing validation only. Built from the 2019 origins that had arrived while the ENS
archives were still downloading, split into three 2019 sub-periods (Jan-Apr train,
May-Aug validation, Sep-Dec test) so the whole chain could be exercised end to end.

191 / 206 / 181 examples, 10 epochs. Nothing here is evidence about path information,
wind power, or any contrast. The real run writes to the sibling directory without the
`_dryrun` suffix.
