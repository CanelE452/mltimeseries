# 2026-09-07

## UCP-PATH-PILOT-v1 완주 — NO_INCREMENTAL_PATH_VALUE

어제 21:37(UTC 13:37) 띄운 `src/run.py` 가 오늘 06:15(UTC 21:15)에 전 구간을 마쳤다.
다운로드 7시간 15분, 나머지 전부 22분.

| 단계 | 소요 |
| --- | --- |
| ENS 54GB 다운로드 + md5 | 7h 15m |
| extract_ens (2,183 origin) | 6.3m |
| build_panel | 7.0m |
| chronos cache | 0.9m |
| 무결성 T01–T13 | 0.5m |
| smoke | 0.1m |
| 학습 21 fit | 6.1m |
| 평가·latency·bootstrap·판정·보고 | 1.3m |

### 판정

```
FINAL TOKEN: NO_INCREMENTAL_PATH_VALUE
```

2021 test, farm equal-weight macro scaled CRPS, 3-seed 평균, 7일 block bootstrap 2000회
(46 block):

| 대비 | RI | 95% CI | seed 양수 |
| --- | --- | --- | --- |
| P over D (primary) | -1.19% | [-2.25, -0.27] | 0/3 |
| P over S (primary) | -2.05% | [-3.73, -0.56] | 0/3 |
| P over P_BROKEN | -0.05% | [-0.26, +0.17] | — |
| S over M | -0.52% | [-1.25, +0.19] | — |
| M over H | **+45.66%** | [+41.70, +48.81] | — |
| P over MC | -0.19% | [-1.48, +0.90] | — |

arm 별 test scaled CRPS: M 0.2995 < S 0.3010 < D 0.3035 < MC 0.3066 < P_BROKEN 0.3070
< P 0.3072 << H 0.5511.

### 읽는 법

**§37 Q1** member path 복원: 됐다 (K=50, 재읽기 bit 동일, T05 통과).
**Q2** 미래 날씨가 H보다 유용한가: 압도적으로 그렇다 (+45.7%, CI 0에서 멀다).
**Q3** ensemble mean 이상의 분포 detail: 아니다 (S over M -0.52%, CI 0 포함).
**Q4** time-wise 분포 이상의 member path: **아니다.** P 가 D보다 나쁘고 CI가 0을 제외한다.
**가장 결정적인 것은 P vs P_BROKEN = -0.05% [-0.26, +0.17]** — 같은 구조·같은 예산으로
경로를 실제로 부순 데이터에 학습시킨 arm과 구별되지 않는다. 경로 연결이 신호를
전혀 싣지 않는다는 뜻이다.
**Q5** MC 대비: 정확도는 같고(-0.19%, CI 0 포함) 지연만 50배 싸다
(batch1 adapter-only P 1.33 ms vs MC 66.9 ms). 정확도 이득이 없으므로 효율만으로
확대할 근거가 아니다.
**Q6** 확대 근거: 없다. 지시문 §9 대로 무거운 path encoder를 덧붙여 주제를 구제하지 않는다.

### 결과 해석을 제한하는 것 두 가지 (사후 변경 없이 기록만)

**1. 어떤 arm도 수렴하지 않았다.** best_epoch 이 21 fit 전부 42–49 of 50 이고 patience 8은
한 번도 발동하지 않았다. 사전 등록한 50 epoch 상한에 전부 걸렸다는 뜻이고, 총 학습은
0.1 GPU-hour — §29 예산 8 GPU-hour 의 1.3% 다. 따라서 이 비교는 "수렴 시점의 비교"가
아니라 "사전 등록된 고정 예산에서의 비교"다. 다만 P 와 P_BROKEN 은 같은 정도로 덜
학습됐으므로 그 대비만큼은 이 한계에 영향받지 않는다.
test 결과를 본 뒤 epoch 를 늘리는 것은 §4·§8 금지사항이라 하지 않았다.

**2. test 표본이 farm 선택 게이트가 약속한 것의 절반이다.** eligibility 는 target 12개
존재만 봤는데, `build_panel` 은 28일 history 결측 10% 상한도 건다. 2021 은 curtailment 로
history 도 자주 뚫려서 1,046 example 이 그 규칙에서 추가로 빠졌다.

| split | example | origin | target_missing 로 탈락 | history_missing 로 탈락 |
| --- | --- | --- | --- | --- |
| train 2019 | 5,547 | 710 | 181 | 0 |
| val 2020 | 4,899 | 719 | 702 | 199 |
| test 2021 | 1,362 | 484 | 3,384 | 1,046 |

farm 별 test example 은 100(T_BEATO-1)–325(T_GANW-13). 게이트 설계의 실수이고,
v2 를 한다면 eligibility 에 history 규칙을 같이 넣어야 한다.

부수적으로 coverage 가 nominal 90%에 못 미친다 (P 0.819, D 0.839, M 0.843). MC 만
0.935 로 과대 — member 조건부 분포 50개를 union 해 mixture 가 넓어진 결과다.

### SELF_AUDIT

15개 항목 전부 통과 (`SELF_AUDIT.md`). A07 permutation invariance는 학습된 checkpoint
에서 재확인, A17/A18 은 metrics.csv 와 per-example loss 에서 재계산해 bootstrap
point estimate 와 대조, A15 는 MC 가 50×32=1600 particle union 인지 실제로 확인.

### 고친 것

`run.py` 마지막 줄이 `SELF_AUDIT.md` 를 인코딩 지정 없이 읽어 cp949 로 죽었다.
모든 산출물이 나온 뒤였고 판정도 로그에 찍힌 뒤였지만, 다음 실행에서 실패로 오독되지
않게 `read_text(encoding="utf-8")` 로 고정했다.
