# 2026-09-12

## Study38 — LoRA-only diagnostic + fresh Stage A preparation

[확인] Before: Study35 JOINT가 WIDE보다8.804323%F0 좋지만 F0보다4.304809%F0 나쁜 개발 결과였다. 추가 residual MLP 없이 native head를 고정한 LoRA-only 결과는 없었다.

[확인] After: `experiments/peft_lora_only_diagnostic_v1/`에서 동일 BMRA/Jena·seeds30000/30001·rank8·96attention projections·720steps를 사용해16trajectory를 완료했다. 추가 probe 없음, pretrained backbone/native output head frozen, 실제 LoRA trainables1179648/192tensors. S180/L720의8선택을 V로 봉인한 뒤8D forecast. 네 zero-update gate 포함 총28guard 정상 종료. Step0 max error0, frozen digest/restore/metric/target/origin 검사 PASS.

[판정] 모든8cell/view CASE C: LoRA-only와 JOINT 모두 F0보다 나쁘다. LoRA-only는 JOINT보다7/8에서 좋지만 BMRA30001 S180에서는 JOINT가7.461550%F0 더 좋다. 추가 head만으로 손해를 설명하기 어렵다. 개발 진단이며 representation 기전·독립 검증·논문 완성을 주장하지 않는다.

[확인] `results/peft_lora_only_diagnostic_v1/`에 raw metrics, 선택 LR/checkpoint, summary, integrity/resource/verification,16fit histories와guard records,2figures,실행 전 setup failure/plan을 보존했다. 결과 노트는 `38_lora_only_diagnostic_20260912.md`. 학습/선택/분석 소스는 D 이전에 봉인했고, D 이후 변경은 결과 보고와 그림 범례 배치 등 presentation뿐이다.

[확인] 최초 admission 로그 폴더 생성 누락은 GPU 시작 전 FileNotFoundError였다. traceback/최초plan을 보존하고 디렉터리 생성 순서만 고친 뒤 재봉인했다. 사용자 요청한 네 LR은 Study35 HEAD/WIDE numeric grid [1e-5,3e-5,1e-4,3e-4]이며, 기존 JOINT LoRA grid [1e-5,3e-5,3e-5,1e-4]와 다름을 PURPOSE/plan/보고서에 명시했다. 학습 중·D 이후 grid/threshold 변경 없음.

[확인] 사용자 “필요 없는것들 지워서 사용할수있게” 승인 후 orphan watcher/helper87개, 미사용 browser automation helper36개와 오래된 유휴 Claude CLI/도우미/빈 shell을 종료했다. 메모리 admission13GiB/runtime6GiB 기준과 OS 설정은 바꾸지 않았다. 원천·학습·sealed 결과 파일 삭제 없음. 최초plan→마지막forecast2957.42s, fit wall합2098.43s, CUDA allocated946.47MiB/reserved964MiB, device sampled2053MiB.

[확인] 기존 Study35 소스·입력59개와 closure provenance486개 해시 불변, 기존32raw D행·V선택 및 새396개 artifact hash 재검산 PASS. 관련 기존 pytest14개 PASS, 새16fit/8forecast의 통합 무결성·수식 검산 PASS. 새 실험 폴더의 `.gitattributes`는 소스의 LF를 고정하고 실제 CRLF로 생성·봉인된 plan은 `-text`로 원래 바이트를 보존한다. Staging 때 발견한 줄바꿈 변환 가능성을 내용 변경 없이 바로잡았다.

[확인] `results/peft_paper_closure_v1/fresh_stage_a_candidate_manifest.{md,json}` 및 read-only 재현 script 추가. Household post-P1은 train48h 결측 창 때문에 BLOCKED; BDG2 Bull Office2016A는 target-blind READY. 별도 source hash/split geometry/각 target 관측률 검산과 원자료 재현 PASS. Fresh 학습·forecast0, pretraining overlap UNKNOWN.

[확인] 사용자 Time-PEFT20쪽 PDF에서 p.6 §5.1.3, p.12 Algorithm1, p.16 §D.1.3/§D.2.2를 확인해 `37_novelty_boundary.md`의 본문 미확보 사유를 갱신했다. HeadOnly/LoRA/FullFT·complexity·frequency/channel adapter·native architecture 보존은 선행 범위다. PDF는 commit하지 않는다. `PAPER_READINESS.md`는 역사적 closure 기록을 보존하며 새 상태를 덧붙였고 MORE_INDEPENDENT_EVIDENCE_REQUIRED 유지.

[판정] 사용자 지시에 따라 main에 직접 commit/push하고 종료한다. 새 branch/PR/force-push/reset/fresh64fit 실행 없음. 기존 미추적 pytest 임시폴더와 tmp는 유지한다.
