# 2026-09-06

## 프로젝트 신설

`E:/CODING/proj/mltimeseries/` 생성. `covariate-trust-pilot` 과 분리된 새 시계열 ML
프로젝트다. 아직 코드·git 저장소 없음.

기존에 `E:/CODING/proj/timeseries/` 가 있었으나 `_docs/history/` 에 auto-compact
마커 3개(각 13줄)뿐이고 코드도 git 저장소도 없는 빈 껍데기라, 재사용 대상이 아니라고
판단해 새 폴더를 만들었다.

폴더 구조는 `proj` 하위의 기존 관례를 따랐다 — `Algorithmic-Trading`,
`covariate-trust-pilot`, `sub`, `timeseries` 모두 `docs/` 가 아니라 `_docs/` 를 쓴다.

```
mltimeseries/
  _docs/
    PROJECT_LOG.md
    reference/
      time_series_research_dossier_20260906.md
      paper_catalog.json
    history/
      2026-09-06.md
```

## 배치한 자료

사용자가 업로드한 문헌 사전조사 2건을 `_docs/reference/` 에 넣었다. 업로드 파일명의
해시 접두어만 떼고 내용은 원본 그대로다.

| 사본 | 원본 | sha256 (앞 16) | 크기 |
| --- | --- | --- | --- |
| `reference/time_series_research_dossier_20260906.md` | `d2fd17fb-time_series_research_dossier_20260906.md` | `5077f87c9f65982d` | 36,910 B |
| `reference/paper_catalog.json` | `f342422b-paper_catalog.json` | `ef9204514c9f6cc6` | 34,862 B |

원본과 사본의 sha256 을 대조해 일치를 확인했다.

내용 요약: 기준일 2026-09-06, 48개 외부 연구/모델/벤치마크 항목, 업로드 포스터 4개
재독해, 5개 후보 연구군. 카탈로그 JSON 은 항목마다
`venue_status` / `mechanism` / `inspection` / `interpretation_limit` / `primary_sources`
를 가진다.

## PROJECT_LOG 에 옮겨 적은 해석 경계

dossier 가 스스로 명시한 제약을 진입점 문서에 옮겼다. 나중에 이 자료를 근거로
"이건 새롭다"거나 "실험을 시작해도 된다"고 오독하는 것을 막기 위해서다.

- 대부분 항목은 채택 상태와 공식 초록 수준의 스크리닝이며 전수 독해·재현이 아니다.
- 성능 수치는 출처 보고값이고 이 조사에서 실행해 확인한 값이 아니다.
- 특정 검색어 기반 사전조사이므로 체계적 문헌고찰이 아니다.
- novelty 확정이나 새 실험 실행 승인의 근거로 쓰지 않는다.

## 옆 저장소와의 접점 기록

`covariate-trust-pilot` 의 `ts-idea-tournament-v1` 브랜치(오늘 실행)가 이 카탈로그의
두 항목을 이미 실험으로 건드렸으므로 PROJECT_LOG 에 포인터를 남겼다. 같은 주제를
다시 시작하기 전에 중복 방지용으로 확인할 것.

- P32 (FAF/AdaRho, AISTATS 2026) — 그 실험 Track F 에서 논문 기준 로컬 구현으로 비교.
  동일 20% 예산에서 AdaRho 가 정상 regime 변화의 72–79% 를 함께 제거했다.
  원 논문의 online selection 을 완전히 재현한 것은 아니다.
- P33 (MTLinear, AISTATS 2025) — 그 실험 Track G 에서 read-only 참조로만 두었다.
  PCGrad·norm-balanced 대조군은 두었으나 MTLinear 자체는 구현하지 않았다.

## 하지 않은 것

- git 저장소 초기화 (요청 범위 밖)
- 연구군 선택, 실험 설계, 코드 작성
- `_docs/notes/` 생성 — 아직 주제축이 없어 빈 폴더를 만들지 않았다

## 포스터 섹션 익명화 + 루트 커밋 재작성

`_docs/reference/time_series_research_dossier_20260906.md` 의 포스터 섹션 헤더 4줄에
발표자 실명과 원본 PDF 파일명이 그대로 들어 있었고, 저장소가 public 이라 이미 GitHub 에
공개된 상태였다. 실명을 지웠고, 학회 포스터 번호도 프로그램과 대조하면 발표자 역추적이
가능해서 함께 지웠다. 문서 등장 순서대로 일련번호를 새로 매겼다.

| Before | After |
| --- | --- |
| `### P2-64-{이름}(1).pdf — 변수별 신뢰도 기반 gradient 조정` | `### 포스터 1 — 변수별 신뢰도 기반 gradient 조정` |
| `### P2-65-{이름}.pdf — Retrieval-Guided Residual Correction` | `### 포스터 2 — Retrieval-Guided Residual Correction` |
| `### P1-65-{이름}.pdf — Adaptive Reference Memory` | `### 포스터 3 — Adaptive Reference Memory` |
| `### P1-64-{이름} (1).pdf — Hurdle Decomposition` | `### 포스터 4 — Hurdle Decomposition` |

본문 내용은 건드리지 않았다. 바뀐 것은 헤더 4줄뿐이다.

`PROJECT_LOG.md` 의 "두 파일은 업로드 원본 그대로다 … 내용을 고치지 않는다" 문장이 이
수정으로 사실과 어긋나게 되어, 무엇을 왜 고쳤는지 명시하도록 같이 갱신했다.

### git 히스토리 재작성

새 커밋으로 지우면 최신 파일에서만 사라지고 최초 커밋 `477a78b` 를 열면 실명이 그대로
보인다. 커밋이 그 하나뿐이었으므로 루트 커밋 자체를 `--amend` 로 다시 쓰고
`--force-with-lease` 로 push 했다. 사용자에게 파괴적 액션임을 알리고 선택을 받았다.

- 재작성 전 백업: 세션 scratchpad 에 `dossier_ORIGINAL_with_names.md` 와
  `mltimeseries_before_rewrite.bundle` (저장소 전체) 를 남겼다. 저장소 안에는 두지 않았다.

### force-push 만으로는 부족했다 — 원격 저장소 삭제 후 재생성

force-push 는 브랜치 포인터만 옮긴다. 참조가 끊긴 옛 커밋 `477a78b` 는 GitHub 에 그대로
남아 SHA 로 계속 열렸다. push 직후 실제로 확인한 값이다.

| 대상 | force-push 직후 |
| --- | --- |
| `raw.githubusercontent.com/.../477a78b/...dossier...md` | HTTP 200, 실명 4건 그대로 |
| `github.com/.../commit/477a78b` | HTTP 200 |

그래서 저장소를 지우고 다시 만들었다. 새 저장소는 저장소 자체가 새 객체 저장소라 옛
커밋이 존재하지 않는다.

삭제 전 확인한 것 — issue·PR·release·star·fork 전부 0, 원격 브랜치는 `main` 하나,
원격 SHA 가 로컬 HEAD 와 동일. 잃을 원격 전용 자산이 없음을 확인한 뒤 진행했다.
로컬 저장소와 scratchpad 의 `mltimeseries_CLEAN_3959a2d.bundle` (`git bundle verify`
로 complete history 확인) 이 백업이다.

`gh repo delete` 는 토큰에 `delete_repo` scope 이 없어 HTTP 403 으로 막혔고, 브라우저
인증이 필요해 사용자가 직접 삭제했다. 재생성·push 는 다시 CLI 로 했다.

```
gh repo create CanelE452/mltimeseries --public --description "..."   # 기존 설명·공개설정 유지
git push -u origin main
```

- 새 저장소 `createdAt` 2026-09-06T02:15:06Z, default branch `main`, visibility PUBLIC.
- push 결과 `* [new branch] main -> main`, 원격 `main` = `3959a2d` (로컬과 동일).
- 새 main 원본을 raw 로 받아 이름 검색 0건 확인.
- 삭제 직후 `github.com/.../commit/477a78b` 는 404 로 바뀌었으나 raw CDN 은 한동안
  200 을 반환했다. `X-Cache: HIT` / `Source-Age: 268` / `max-age=300` 으로, origin 이
  아니라 Fastly 엣지 캐시였다. 캐시 만료까지 폴링해 404 를 확인했다.

## TiRex-2 를 이 Windows PC 에서 돌 수 있는지 실측

사용자 질문: "tirex2 윈도우에서 못 돌려? 우분투여야돼?"

앞선 턴에서 내가 "Windows 에서 그대로는 못 돌린다"고 한 것은 공식 README 만 읽은 추정이었다.
실제로 설치해서 돌려 보니 CPU 는 된다. 아래는 전부 실행 결과다.

### 이 PC 조건

Windows 11 (10.0.26200), RTX 4070 12GB (compute capability 8.9), conda base python 3.13.
`cl.exe` 없음, `nvcc` 없음, Docker 없음, WSL 없음 — 넷 다 `where`/`ls` 로 확인.

### 만든 환경

conda env `mlts` (python 3.11.16). base 는 `libiomp5md.dll` 이 중복 초기화되는 OpenMP 충돌이
있어 재사용하지 않았다.

```
conda create -y -n mlts python=3.11
pip install torch --index-url https://download.pytorch.org/whl/cu128   # 2.11.0+cu128, 2.75GB
pip install tirex-2                                                    # 0.2.1
```

`tirex_2-0.2.1-py3-none-any.whl` 은 순수 python 휠이고 OS classifier 도 없다. 패키지 자체엔
플랫폼 제약이 없다. 막히는 것은 의존성 쪽이다.

### 막힌 지점 3개와 각각의 정체

1. **import 자체가 실패** — `xlstm/blocks/slstm/src/cuda_init.py:34` 가 모듈 최상위에서
   `torch.utils.cpp_extension.include_paths(device_type="cuda")` 를 호출한다.
   `CUDA_HOME` 이 없으면 `OSError`. `device` 인자를 보기도 전이라 cpu/cuda 둘 다 죽는다.
   해당 블록은 `if torch.cuda.is_available():` 안에 있고 하는 일은 `CUDA_LIB` 경로 문자열
   계산뿐이다. 그래서 `CUDA_HOME` 을 아무 실재 디렉터리로 지정하면 통과한다 (실측).

2. **CPU 인데 `cl` 을 찾음** — `tirex2/model/component/residual_block.py:9` 의 `@torch.compile`
   과 `attention_block.py:45` 의 `torch.compile(flex_attention)` 때문에 TorchInductor 가
   C++ 컴파일러를 부른다. `InductorError: Compiler: cl is not found`.
   `TORCH_COMPILE_DISABLE=1` 로 eager 실행하면 통과한다 (실측).
   (앞서 "tirex2 에 torch.compile 없음"이라고 적었던 것은 20여 파일 중 2개만 grep 한 오류다.)

3. **CUDA 는 fused sLSTM 커널을 런타임 컴파일** — `flashrnn` 이 `torch.utils.cpp_extension.load`
   를 부른다. `ninja` 는 env 의 `Scripts/` 에 설치돼 있으나 PATH 에 없어 먼저 실패했고,
   PATH 에 넣으면 다음 단계인 `where cl` 에서 실패한다. MSVC 가 필요하다.

### 되는 조합

```bash
export CUDA_HOME="C:/Users/User/anaconda3/envs/mlts"   # 아무 실재 디렉터리면 됨
export TORCH_COMPILE_DISABLE=1
python -c "from tirex2 import load_model; m = load_model('NX-AI/TiRex-2', device='cpu')"
```

정확성 확인 — context 를 `sin(t/8)`, t=0..127 로 주고 32스텝 예측.

| 스텝 | 정답 `sin(t/8)` | 모델 median |
| --- | --- | --- |
| t=128 | -0.2879 | -0.283 |
| t=129 | -0.3944 | -0.394 |

### CPU 속도 (context 512 -> horizon 96, batch 1, 3회 평균)

| 변량 수 | 시간 |
| --- | --- |
| 1 | 87 ms |
| 7 (ETTm2 규모) | 271 ms |
| 21 (Weather 규모) | 847 ms |

batch 를 키웠을 때의 amortize 효과는 재지 않았다. 위 수치는 `forecast([ts])` 단건 기준이다.

### GPU 우회 시도 — Triton 백엔드는 이 GPU 에서 실패

`flashrnn` 에는 `vanilla` / `vanilla_fwbw` / `cuda` / `cuda_fused` / `triton_fused` 백엔드가
있고, `tirex2/model/component/flashrnn_slstm.py:341` 의 `_flashrnn_backend()` 가 device 로
`cpu|mps -> vanilla`, `cuda -> cuda` 를 하드코딩한다. Triton 3.8.0 이 Windows torch 휠에
포함돼 있어 이 함수를 monkeypatch 해 `triton_fused` 로 돌려봤다.

커널 컴파일까지는 갔으나 실행에서 죽었다.

```
OutOfResources: shared memory, Required: 270336, Hardware limit: 101376
```

RTX 4070 (Ada) 의 SM 당 공유 메모리 한계를 넘는다. block size / num_stages 를 줄이면
될 수도 있으나 커널 튜닝이 필요한 영역이라 여기서 멈췄다.

### 결론

- 우분투가 필수는 아니다. Windows 에서 CPU 추론은 환경변수 2개로 실제로 된다.
- GPU 를 쓰려면 MSVC Build Tools + CUDA Toolkit 12.8 설치가 필요하다.
  FlashRNN CUDA 커널이 Windows 에서 실제로 빌드되는지는 아직 미확인이다.
- WSL2 + 우분투가 가장 확실한 GPU 경로다. 이 PC 엔 WSL 이 설치돼 있지 않다.

## 모델 3종 + 데이터셋 3종 설치와 스모크 테스트

사용자 요청: Chronos-2 / 공식 PatchTST / TiRex-2 와 ETTm2 / Weather / Electricity 를 가져오고,
"일단 설치해서 되는지 확인해봐".

### 새로 만든 것

| 경로 | 내용 |
| --- | --- |
| `scripts/fetch_data.py` | 벤치마크 3종을 HF 에서 받고 공표된 shape 와 대조 |
| `scripts/smoke_test.py` | 실제 ETTm2 윈도우로 모델 3종을 각각 한 번씩 돌림 |
| `third_party/PatchTST/` | 공식 저장소 clone (gitignore 대상) |
| `data/` | 벤치마크 CSV 3종 (이미 gitignore 대상) |

`scripts/` 는 새로 만들었다. 프로젝트에 코드 폴더가 없었고, 재실행 가능해야 하는 절차라서다.
`.gitignore` 에 `third_party/` 를 추가했다 — 상류 저장소는 vendoring 하지 않는다.

### 설치

기존 conda env `mlts` 재사용. `pip install "chronos-forecasting>=2.0" pandas pyarrow` 로
`chronos-forecasting 2.3.1` 설치. 컴파일 단계 없음 — TiRex-2 와 달리 순수 python 경로다.

공식 PatchTST 는 pip 패키지가 없어 저장소를 clone 했다 (`yuqinie98/PatchTST`,
`PatchTST_supervised/` 와 `PatchTST_self_supervised/` 포함).

### 데이터셋 — 공표된 shape 와 전부 일치

출처는 Time-Series-Library README 가 가리키는 HF 미러 `thuml/Time-Series-Library` 다.
Autoformer/PatchTST 계열 논문이 쓰는 것과 같은 CSV 라 수치 비교가 성립한다.

| 파일 | 행 | 변량 | 간격 | 기간 | NaN | 크기 |
| --- | --- | --- | --- | --- | --- | --- |
| `data/ETT-small/ETTm2.csv` | 69,680 | 7 | 15분 | 2016-07-01 ~ 2018-06-26 | 0 | 9.7 MB |
| `data/weather/weather.csv` | 52,696 | 21 | 10분 | 2020-01-01 ~ 2021-01-01 | 0 | 7.2 MB |
| `data/electricity/electricity.csv` | 26,304 | 321 | 1시간 | 2016-07-01 ~ 2019-07-02 | 0 | 95.6 MB |

행 수·변량 수를 논문 보고값과 assert 로 대조한다. 미러가 조용히 다른 파일을 주면 실패한다.

### 스모크 테스트 결과 — 4/4 동작

ETTm2 마지막 구간에서 context 512 -> horizon 96, 7변량 동시 예측.

| 실행 | 결과 | 시간 |
| --- | --- | --- |
| chronos-2 (cuda) | 동작 | 1.4s |
| chronos-2 (cpu) | 동작 | 1.1s |
| tirex-2 (cpu) | 동작 | 1.1s |
| PatchTST (cpu, 미학습) | 동작 | 0.0s, 115,875 params |

Chronos-2 는 GPU 에서 그대로 돈다. TiRex-2 처럼 커널을 런타임 컴파일하지 않기 때문이다.

**이 표의 MAE 를 성능 비교로 읽으면 안 된다.** 윈도우 1개(표본수 1), 정규화 없는 원 스케일,
PatchTST 는 무작위 초기화 상태다. 확인한 것은 "이 PC 에서 실제로 forecast 가 나온다"뿐이다.
정식 평가는 train/val/test 분할·정규화·rolling window 를 갖춘 별도 작업이다.

### 도중에 고친 것

첫 실행에서 Chronos-2 가 두 번 다 실패했다. 내가 쓴 스크립트가 `predict()` 에
`quantile_levels` 를 넘겼는데 그 인자는 `predict_df()` 쪽이다. 실제 시그니처는
`(batch, n_variates, history)` 를 받아 `(n_variates, n_quantiles, horizon)` 리스트를 돌려주고,
quantile 레벨은 `pipeline.quantiles` 에 있다. 두 모델 모두 레벨 목록에서 0.5 를 찾아
median 을 뽑도록 고쳤다 (인덱스 하드코딩 제거).

### 실행 방법

```bash
export CUDA_HOME="C:/Users/User/anaconda3/envs/mlts"   # xlstm 이 import 시점에 읽는다
export TORCH_COMPILE_DISABLE=1                          # TiRex-2 CPU 경로가 MSVC 를 찾는 것 회피
python scripts/fetch_data.py
python scripts/smoke_test.py
```

Chronos-2 만 쓸 때는 두 환경변수가 필요 없다. TiRex-2 를 import 하는 순간 필요해진다.

### 알아둘 것

`.gitignore` 의 `data/` + `!data/.gitkeep` 조합은 git 규칙상 동작하지 않는다 (부모 디렉터리가
제외되면 하위를 다시 포함할 수 없다). `data/` 는 `scripts/fetch_data.py` 가 만드므로 실害는
없다. 고치려면 `data/*` 형태여야 한다.

## HQ-TOKEN-PILOT-v1 구현과 실행

사용자가 `01_forecast_query_tokenization_CLI.txt` (예측 목표 조건부 토큰화 최소 비교실험
지시문)을 주며 "이거 해줄수있어?"라고 물었고, 실행까지 요청했다.

### 착수 전 확인한 것

지시문 §2가 요구하는 공식 구현 URL 12개를 `curl` 로 전부 확인했다 — 전부 HTTP 200.
`third_party/PatchTST` 의 공식 loader 를 직접 읽어 지시문의 split 경계가 실제 코드와
일치함을 대조했다.

| 확인 대상 | 공식 코드 위치 | 결과 |
| --- | --- | --- |
| ETTm2 경계 34560/46080/57600 | `data_loader.py:136-137` (`12*30*24*4` 등) | 일치 |
| weather·electricity 0.7/0.2 규칙 | `data_loader.py:234-238` | 일치 |

PatchTST clone commit `204c21efe0b39603ad6e2ca640ef5896646ab1a9`, Apache-2.0. 읽기만 했고
이 실험은 PatchTST 코드를 import 하지 않는다.

### 실측한 자원 (지시문 가정과 크게 다름)

지시문 §11 은 12시간 wall cap 과 10시간 초과 시 예산 축소 분기를 둔다. 실제로는:

| 항목 | 실측 |
| --- | --- |
| fit 1개 학습 (3000 update) | 5.4 ms/update → 0.3분 |
| 전체 27 fits 순수 학습 | 약 10분 |
| 평가 window (3 dataset 합) | val 19,230 / test 39,516, pass당 0.1초 |
| driver smoke 재측정 | 11.3 ms/update, core 추정 0.26시간 |

`REDUCED_BUDGET_SCREEN` 분기는 발동하지 않았다.

### 만든 파일

| 경로 | 내용 |
| --- | --- |
| `experiments/hq_token_pilot_v1/data.py` | CSV 로드·split·정규화·origin 격자·재현 가능한 학습 스케줄 |
| `experiments/hq_token_pilot_v1/model.py` | §6 공통 모델 + §7 여섯 arm (U/I/C/R/H_STATIC/DENSE) |
| `experiments/hq_token_pilot_v1/train.py` | 학습 루프·checkpoint 선택·§11 자원 감시 스레드 |
| `experiments/hq_token_pilot_v1/evaluate.py` | 키 기반 평가·지표·블록 부트스트랩·§13 진단·§15 latency |
| `experiments/hq_token_pilot_v1/run.py` | smoke→freeze→검증→27 fits→분석→판정 전 과정 드라이버 |
| `experiments/hq_token_pilot_v1/report.py` | §21 STATUS.md 작성 (분석 없음, 보고 전용) |
| `experiments/hq_token_pilot_v1/PURPOSE.md` | 소비처·문장 2줄, 노트를 가리킴 |
| `tests/hq_token_pilot_v1/test_invariants.py` | §8·§20 불변식 25개 |
| `results/hq_token_pilot_v1/sources.json` | §2 출처 확인 결과 |

`_docs/notes/` 의 노트 3개를 `_docs/notes/tsfm_topics/` 로 옮겼다 (지시문 §1 과 노트
frontmatter 의 `suggested_repository_path` 가 둘 다 그 경로를 가리킨다). 새 폴더를 만들지
않고 기존 노트를 이동했다.

branch `hq-token-pilot-v1` 생성. 지시문 §1 은 별도 worktree 를 요구하지만 만들지 않았다 —
노트·`data/`·`third_party/` 가 이 작업 트리에서 untracked 이거나 gitignore 대상이라
`origin/main` 에서 딴 worktree 에는 그것들이 없고, 복사하면 중복 폴더가 생긴다.

### 저장소 지정이 지시문과 달랐다

지시문 §1 은 `CanelE452/covariate-trust-pilot` 을 지정하는데 노트·데이터·환경은 전부
`CanelE452/mltimeseries` 에 있다. 사용자에게 물어 `mltimeseries` 로 확정했다.
편차는 `execution_spec.json` 의 `repository_deviation` 에 기록했다.

### 불변식 검증 결과

`pytest tests/hq_token_pilot_v1/` — 25 passed. §8 의 11개 항목(그룹 softmax 합=1,
encoder 입력 길이가 실제로 B, B=64 항등성, I 의 pool query 무효과, C scorer gradient 전달,
미래값 비누수, 키 join 복원)과 §20 의 통합 test 를 모두 포함한다.

작성 중 test 쪽 버그 1건 — 부트스트랩 성공 반환에는 `macro` 키가 없는데 테스트가 그걸
읽어 `KeyError`. `macro_mean`/`lower95` 를 보도록 고쳤다.

### 판별자(plan-critic) 지적 반영

깨끗한 컨텍스트의 `plan-critic` 에 지시문 전문을 넘겨 검증했다. 20개 지적 중 4개를
반영했다. 사전등록 수치(arm·임계값·split·격자·예산)는 **하나도 바꾸지 않았고**, 측정만
추가했다.

| 지적 | 대응 |
| --- | --- |
| 압축 여유폭 확인 관문 없음 | DENSE−U 간격을 전제 조건 수치로 보고. 두 arm 다 이미 §10 목록에 있어 새 학습 없음 |
| 양성 대조군 없음 | 합성 과제(짝수 patch=짧은 미래, 홀수 patch=긴 미래)에서 C가 I를 이기는지 사전 확인 |
| seed 노이즈가 CI에 없음 | arm I 를 8 seed 로 validation 에서만 돌려 표준편차 측정. 판정 입력 아님 |
| 평가 격자 위상이 일주기와 정렬 | 사전등록 격자를 primary 로 두고 위상 이동 격자 재평가를 robustness 행으로 병기 |

반영하지 않은 것 — 데이터 출처 BLOCKED_SOURCE 판정(§4 의 "또는 동일 데이터임을 검증할 수
있는 로컬 사본" 절에 해당), max_updates 상향·격자 교체·arm 추가·§14 판정식 재작성(전부
사전등록 계약 변경). electricity 가 0.035 epoch 만 본다는 사실은 `known_limitations` 에
한계로 기록했다.

§3 freeze 에서 지시문의 모호구간 3개를 확정했다 — `-1.0%` 는 dataset별 horizon 평균 RI,
`lower95` 는 양측 95%의 2.5 percentile, primary RI 추정량은 seed 평균 오차의 비.

### dry-run

`--dry-run` (20 update, 별도 출력 폴더)으로 전 경로를 0.9분에 완주시켜 배선을 확인했다.
판정 조건 8개 평가, electricity `LOW_EFFECTIVE_TIME_BLOCKS` 플래그(4 블록), 여유폭 경고가
모두 정상 발화했다. 그 수치는 20 update 산물이라 과학적 의미가 없다.
산출물은 `results/hq_token_pilot_v1_dryrun/` 에 남아 있다 (커밋 대상 아님).

### 본 실행

`python -u -m experiments.hq_token_pilot_v1.run` 을 백그라운드로 띄웠다. 로그는
`runs/hq_token_pilot_v1/driver.log`. 스크립트가 smoke→freeze→양성대조→seed노이즈→27
fits→분석→부트스트랩→진단→latency→STATUS.md 판정까지 한 번에 이어진다.

## 실행 결과와 판정

### 실행 결과

`execution_status = COMPLETE` (27/27 fits, 38.3분), `scientific_decision = INCONCLUSIVE`.
사전등록 8개 조건 중 3개 통과. 전체 보고서는 `results/hq_token_pilot_v1/STATUS.md` (15개 절).

주요 대조 C vs I macro **-0.037%**, 95% 구간 [-0.128, +0.033]. 사전등록 문턱은 +1.0%.

| 대조 | macro |
| --- | --- |
| DENSE vs U (압축 여유폭) | +3.72% |
| C vs U | +2.84% |
| C vs H_STATIC | +1.89% |
| C vs I (주요) | -0.04% |
| C vs R (가짜 horizon) | -0.01% |
| 위상 이동 격자 | -0.14% |

결정적 진단 — 실데이터에서 C 의 pooling 가중치가 horizon 에 따라 거의 안 움직였다.

| dataset | 평균 \|Δw\| (96 vs 336) | 토큰 중심 이동 |
| --- | --- | --- |
| ETTm2 | 0.00051 | 0.008 샘플 |
| weather | 0.00122 | 0.020 샘플 |
| electricity | 0.00135 | 0.022 샘플 |

같은 구조가 합성 양성대조군에서는 0.813 움직였다 (`CAPACITY_CONFIRMED`, H=96 개선 +50.7%).

효율 — 32 토큰 압축이 전체 지연을 줄이지 못했다. DENSE(64토큰) 1.97ms 대 C(32토큰) 2.73ms.
scorer 비용이 짧아진 시퀀스가 아끼는 것보다 컸다. 1.5M 파라미터 모델에 한정된 관찰이다.
지시문 §15 가 경고한 "attention FLOPs 감소 ≠ 추론 가속"이 실제로 일어났다.

seed 노이즈 측정 (arm I, 8 seed, validation only) — ETTm2 5.05%, weather 0.96%,
electricity 0.78%. ETTm2 는 1.0% 문턱이 애초에 해상도 밖이었다.

### 합성 양성대조군을 세 번 다시 설계했다

첫 설계(백색잡음 patch 의 합)는 두 arm 모두 MSE 0.98 로 아무것도 학습하지 못했다.
목표 분산이 0.128 이라 0 을 예측하는 게 거의 최적이었다. 단위분산으로 재척도해도 같았다 —
백색잡음의 정확한 선형 범함수라 학습 신호가 없었다.

같은 구조로 실데이터를 돌려 원인을 갈랐다. ETTm2 3000 update 후 test MSE 0.2235 로
seasonal-naive 0.2660 을 이겼다. 구조 문제가 아니라 과제 문제였다.

두 번째 설계(짝수 patch 전체가 공유 레벨 α, 홀수가 β)는 두 arm 모두 MSE 0.0095 로 풀었다.
32개 그룹에 역할을 공간적으로 분배하면 horizon 없이도 두 정보를 다 보존할 수 있어서다.
**이건 검사의 결함이 아니라 메커니즘에 대한 사실이다** — 공간 분배가 horizon 조건화의
실질적 대안이고, H_STATIC arm 이 그걸 재는 자리다.

세 번째(그룹마다 독립 목표값 32개)는 decoder 가 예산 안에 아무것도 못 배웠다.

네 번째가 통과했다 — 그룹마다 독립 레벨 a_g, b_g, 짧은 미래는 32개 a 의 평균,
긴 미래는 32개 b 의 평균. a 가 서로 독립이라 공간 분배로 우회할 수 없다.
C 가중치 0.999(H=96) 대 0.147(H=336), 분리 0.813, RI +50.7%.

### §16 문헌 baseline — 4개 전부 공식 구현 못 찾음

| 방법 | 확인 방법 | 결과 |
| --- | --- | --- |
| Local Token Merging (2025/ICML) | PMLR 랜딩 + 21쪽 PDF 본문·링크 annotation 스캔 | 유일한 GitHub 링크가 `Lyken17/pytorch-OpCounter` 인용 |
| BPE for Time Series (2026/ICML) | arXiv 초록 + 32쪽 PDF | 저장소 링크 없음 |
| TimeSqueeze | arXiv 초록 + 21쪽 PDF | 저장소 링크 없음 |
| PATK (2026/AAAI) | AAAI OJS 랜딩 | 코드 링크 없음 |

`MISSING_STRONG_DYNAMIC_BASELINE` 으로 기록. 자체 휴리스틱을 만들어 재현이라 부르지 않았다.

### §17 foundation reference

선언한 부분격자(ETTm2 117×7, weather 107×21, electricity 52×16 채널 subsample)에서
core arm 을 같은 키로 다시 채점해 나란히 뒀다.

| dataset | H | C(32tok) | I(32tok) | DENSE(64tok) | Chronos-2 | TiRex-2 |
| --- | --- | --- | --- | --- | --- | --- |
| ETTm2 | 96 | 0.20641 | 0.20561 | 0.21466 | 0.17491 | 0.16971 |
| ETTm2 | 336 | 0.29441 | 0.29444 | 0.30071 | 0.29972 | n/a |
| weather | 96 | 0.16274 | 0.16269 | 0.16223 | 0.14798 | 0.14765 |
| weather | 336 | 0.26103 | 0.26085 | 0.25660 | 0.25859 | n/a |
| electricity | 96 | 0.15363 | 0.15369 | 0.14746 | 0.12665 | 0.12763 |
| electricity | 336 | 0.18577 | 0.18580 | 0.18127 | 0.15775 | n/a |

Chronos-2 는 GPU 에서 COMPLETE. quantile 21개 중 0.5 를 썼으므로 median 이다 —
평균으로 학습한 core arm 을 MSE 로 재는 것과 조건이 다르다.

TiRex-2 는 `PARTIAL_HORIZON_UNSUPPORTED`. 두 가지 이유가 겹쳤다.

1. GPU 불가 (flashrnn 이 fused sLSTM CUDA 커널을 런타임 컴파일, MSVC·CUDA Toolkit 없음).
   CPU 로 돌렸고 H=96 3,898 window 에 약 4분 걸렸다.
2. **최대 예측 길이가 320 스텝**이라 H=336 을 지원하지 않는다. 실제로 확인한 값이다
   (`prediction_length=336 exceeds the supported maximum of 320`). 336 을 요청하면 320 만
   돌려준다. autoregressive 로 이어붙이는 것은 저자가 정의하지 않은 사용법이라 하지 않고
   `BLOCKED_CONTRACT` 로 남겼다.

pretrained 모델이 fine-tuned pilot 을 대체로 앞섰다. 학습 조건이 다르고
(zero-shot 대 target-fitted) pretraining 중복도 확인하지 않았으므로 비교로 쓰지 않는다.

### 도중에 고친 것

- TiRex-2 호출에 numpy 배열을 그대로 넘겨 `AttributeError: 'numpy.ndarray' object has no
  attribute 'target'`. `TimeseriesType(target=..., past_covariates=None, future_covariates=None)`
  으로 감싸야 한다. quantile 은 9개이고 0.5 는 index 4.
- Chronos-2 는 `(n_variates, 21 quantiles, horizon)` 을 돌려준다. `pipeline.quantiles` 에서
  0.5 를 찾아 index 10 을 썼다 (하드코딩 안 함).
- 불변식 test 1건이 부트스트랩 성공 반환에 없는 `macro` 키를 읽어 KeyError.

### 산출물

`results/hq_token_pilot_v1/` — STATUS.md, verdict.json, execution_spec.json(+sha256),
data_manifest.json, environment.json, sources.json, metrics.csv, fit_manifest.csv,
contrasts.json, bootstrap.json, diagnostics.json, efficiency.csv, optional_references.json,
seed_noise_floor.json, mechanism_capacity_check.json, figures/ (3개 + captions.md).

원격 push 는 하지 않았다. commit 도 아직 하지 않았다.


## 결과 그림 3종 (viz-expert)

`experiments/hq_token_pilot_v1/figures.py` 신규. 동결된 결과 파일만 읽어
`results/hq_token_pilot_v1/figures/` 에 PNG 3장 + `captions.md` 를 만든다 (분석 모드,
`~/.claude/agents/viz-expert/assets/analysis.mplstyle` + `palette.py`, 170 dpi).

- `fig1_primary_contrast.png` — C vs I 6셀 forest(부트스트랩 95% 구간) + macro + 위상이동
  격자 macro. 왼쪽은 사전등록 관문 +1.0% 가 보이는 전체 스케일, 오른쪽은 3.2배 확대.
  electricity 두 셀은 유효 블록 4개라 흰 속 마커로 구분.
- `fig2_accuracy_vs_latency.png` — 실행한 6개 config 만(B=32 다섯 arm + B=64 DENSE),
  dataset×horizon 6패널. x=end-to-end median latency(batch 64), y=test MSE.
  seed 2026090601 채운 마커 / seed 2026090602 빈 마커를 선으로 이어 seed 폭을 병기.
- `fig3_pooling_weights.png` — 32 그룹별 H=96 대 H=336 pooling 가중치(첫 원소).
  왼쪽 실데이터 3종, 오른쪽 합성 양성대조군. 두 패널 y축 눈금 공유(sharey).

부수 수정: `~/.claude/agents/viz-expert/assets/palette.py` 의 `verify()` 가
`read_text()` 를 인코딩 없이 호출해 한국어 Windows(cp949)에서 UnicodeDecodeError 로
죽었다. `encoding='utf-8'` 명시로 수정 (전역 설정이라 commit/push 는 하지 않음).

### execution_spec.sha256 가 파일과 안 맞았다

최종 점검에서 `execution_spec.json` 의 sha256 을 다시 계산하니 companion 파일과 달랐다.

| | 값 |
| --- | --- |
| 파일 그대로 | `16a82bd2833779...` |
| 줄바꿈을 LF 로 바꾼 뒤 | `5640e72140efab...` |
| 기록된 값 | `5640e72140efab...` |

원인은 Windows 텍스트 모드다. 해시는 `json.dumps` 가 만든 LF 문자열로 계산했는데
`open(path, "w")` 가 파일에 CRLF 를 썼다. 내용은 같고 줄바꿈만 달랐지만, 지시문 §3 이
요구하는 "companion 파일로 검증 가능한 해시"가 성립하지 않는 상태였다.

`run.py` 를 바이트 모드(`"wb"`)로 고치고, 기존 산출물의 줄바꿈을 LF 로 정규화했다.
재계산 결과 기록값과 일치한다.

`execution_spec.json` 의 `code_hash = 324c04b66d7d68da...` 는 freeze 시점(실행 직전)에
5개 모듈로 계산한 값이다. 커밋된 `run.py` 는 그 뒤 위 CRLF 수정이 한 건 들어가 해시가
다르다. 그 수정은 spec 파일을 바이트로 쓰는 것뿐이고 학습·평가·판정 경로를 건드리지
않는다. 지시문 §19 대로 영향받은 stage(spec 파일 기록)만 다시 수행했고, 나머지 산출물은
재계산하지 않았다. `figures.py` 와 `references.py` 는 core 이후에 만들어진 파일이라
freeze 시점 해시 대상이 아니다.
